'use client';

import React from 'react';

// This is a simple placeholder for any user role that doesn't have a specific dashboard.
const DefaultDashboard = () => {
  return (
    <div className="dashboard-container">
      <div className="dashboard-header">
        <h1>Dashboard</h1>
        <p>Welcome! Your specific dashboard is under construction.</p>
      </div>
    </div>
  );
};

export default DefaultDashboard;